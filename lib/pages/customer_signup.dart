import 'package:flutter/material.dart';

class CustomerSignupUI extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("data"),
    );
  }
}